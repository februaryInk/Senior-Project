function Control ( action, controlPanel, editor, selector, textarea ) {
    
    this.action = action;
    this.controlPanel = controlPanel;
    this.editor = editor;
    this.selector = selector;
    this.textarea = textarea;
}
