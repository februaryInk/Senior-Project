if ( <%= @saved %> ) {
    window.location = '<%= conversation_path( @conversation.id ) %>';
}
else {
    $( '#form-comment-new' ).html( "<%= escape_javascript( render( :partial => 'comments/new' ) ).html_safe %>" );
}