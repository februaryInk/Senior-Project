if ( <%= @saved %> ) {
    window.location = '<%= user_path( params[ :user_id ] ) %>';
}
else {
    $( '#form-post' ).html( "<%= escape_javascript( render( :partial => 'posts/new' ) ).html_safe %>" );
}